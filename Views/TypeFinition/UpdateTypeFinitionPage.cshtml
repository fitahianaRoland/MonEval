@{
    ViewData["Title"] = "Home Page";
}
<div class="list_container">
    <a asp-action="ListTypeFinitionPage" asp-controller="TypeFinition" class="bouton_lien">
        <div class="interne_bouton">
            <i class='bx bx-chevron-left-circle' style="height: 11px;"></i>
            <label>retour</label>
        </div>
    </a>
    <div class="title_content">
        <p>modifier finition</p>
    </div>
</div>


<div id="formulaire_content">
        <div id="item_content">
            <form asp-action="UpdateTypeFinitionBase" asp-controller="TypeFinition">
                <input type="hidden" name="id" value="@ViewBag.listTypeFinition.Id">
                <div class="champ">
                    <p>nom<span>*</span></p>
                    <input type="text"  name="nom" value="@ViewBag.listTypeFinition.Nom" required>
                </div>
                <div class="champ">
                    <p>augmentation<span>*</span></p>
                    <input type="number"  name="pourcentage" value="@ViewBag.listTypeFinition.Pourcentage" required>
                </div>
                @if (!string.IsNullOrEmpty(ViewData["ErrorMessage"] as string))
                    {
                        <div><label 
                        style="color: red;font-size: small; text-decoration: underline;">
                        @ViewData["ErrorMessage"]
                        </label>
                        </div>
                    }
                <button type="submit">Update</button>
            </form>
            @* <div class="not_registered">
                <p><span>Have an account yet?</span><span class="create_account"><a href="@Url.Action("LoginPage", "Login")" style="text-decoration:none;"> login?</a></span></p>
            </div> *@
        </div>
    </div>